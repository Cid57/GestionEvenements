{% extends 'base.html.twig' %}

{% block title %}Créer Nouvel Événement{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header">
        <h1>Créer Nouvel Événement</h1>
    </div>
    <div class="card-body">
        <div class="form-container">
            {{ include('evenement/_form.html.twig') }}
        </div>
    </div>
    <div class="card-footer">
        <a class="btn btn-secondary" href="{{ path('app_evenement_index') }}">
            <i class="fas fa-arrow-left"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}